# Save exact versions, don't use ^ or ~
save-exact=true

# Don't automatically update package-lock
package-lock=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Don't fund anything
fund=false

# Don't audit during install (optional)
audit=false
